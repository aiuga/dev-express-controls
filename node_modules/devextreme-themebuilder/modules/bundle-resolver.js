"use strict";

module.exports = function (theme, colorScheme) {
    colorScheme = colorScheme.replace(/-/g, ".");
    var themePart = theme !== "generic" ? theme + "." : "";
    return "bundles/dx." + themePart + colorScheme + ".less";
};